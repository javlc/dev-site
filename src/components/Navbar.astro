---
import ThemeIcon from "../../public/themeIcon.svg";

const items = [
    {
      title: 'Home',
      path: '/',
    },
    {
      title: 'Services',
      path: '/services',
    },
    {
      title: 'Work',
      path: '/work',
    },
    {
      title: 'Contact',
      path: '/contact',
    },
    {
       title: 'Blog',
       path: '/blog',
    },
];
---

<div>
   <div>
      <ul class="flex bg-zinc-200 dark:bg-gray-500">
         <li class="basis-2/5">
            <a href=""
               ><img
                  src="/logo.svg"
                  alt="Aptus Technica. Click for home."
                  class="logo h-12"
               /></a
            >
         </li>
         {
            items.map((item) => (
               <li>
                  <a href={item.path}>{item.title}</a>
               </li>
            ))
         }
         <li><button id="theme-toggle" class="text-white font-semibold rounded-lg shadow-md hover:bg-primary-dark focus:outline-none focus:ring-2 dark:focus:ring-2 focus:ring-primary-lighter dark:focus:ring-primary focus:ring-opacity-75"><ThemeIcon /></button></li>
      </ul>
   </div>
</div>
<script>

</script>
<style is:global>
  [data-theme] #theme-toggle:hover > svg {
    fill: white;
  }
  [data-theme="light"] #theme-toggle > svg > .sun,
  [data-theme="dark"] #theme-toggle > svg > .moon {
    fill: none;
  }
</style>